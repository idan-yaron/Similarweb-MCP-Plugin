---
description: Inspect, refresh, or reset the Similarweb plugin's capability map.
argument-hint: [--show | --refresh | --reset | --schedule-grounding <weekly|monthly|off>]
allowed-tools:
  - Read
  - Write
  - Bash
---

The user invoked `/sw-config $ARGUMENTS`. Follow the `sw-config` skill in `skills/sw-config/SKILL.md` to fulfill the request, including its argument-parsing, capability-gating, and output-rendering contracts.
