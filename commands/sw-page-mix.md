---
description: URL-level and folder-level content surface for a domain.
argument-hint: <domain> [--country <iso2>] [--folder-depth <N>]
allowed-tools:
  - Read
  - Write
  - Bash
---

The user invoked `/sw-page-mix $ARGUMENTS`. Follow the `sw-page-mix` skill that ships in this plugin (load it by name via the platform's skill mechanism, or read its SKILL.md from the plugin's installed skills directory, not the current working directory) to fulfill the request, including its argument-parsing, capability-gating, and output-rendering contracts.
