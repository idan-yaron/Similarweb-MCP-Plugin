---
description: Market-size profile for a category or keyword cluster.
argument-hint: <category-or-keyword-cluster> [--country <c>] [--amazon-tld <tld>] [--web-companion] [--enrich-traffic]
allowed-tools:
  - Read
  - Write
  - Bash
---

The user invoked `/sw-market-size $ARGUMENTS`. Follow the `sw-market-size` skill that ships in this plugin (load it by name via the platform's skill mechanism, or read its SKILL.md from the plugin's installed skills directory, not the current working directory) to fulfill the request, including its argument-parsing, capability-gating, and output-rendering contracts.
