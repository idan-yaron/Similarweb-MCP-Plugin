---
description: Keyword gap analysis between a target domain and a competitor.
argument-hint: <domain> --vs <competitor> [--country <iso2>]
allowed-tools:
  - Read
  - Write
  - Bash
---

The user invoked `/sw-keyword-opportunity $ARGUMENTS`. Follow the `sw-keyword-opportunity` skill that ships in this plugin (load it by name via the platform's skill mechanism, or read its SKILL.md from the plugin's installed skills directory, not the current working directory) to fulfill the request, including its argument-parsing, capability-gating, and output-rendering contracts.
